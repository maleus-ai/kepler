kepler:
  logs:
    buffer_size: 0
  hooks:
    pre_stop:
      run: |
        echo "PRE_STOP_RAN" >> __MARKER_FILE__
    post_stop:
      run: |
        echo "POST_STOP_RAN" >> __MARKER_FILE__

services:
  quiescent-service:
    command: ["sh", "-c", "exit 0"]
    restart: "no"
