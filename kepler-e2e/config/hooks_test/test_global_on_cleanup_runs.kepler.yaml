kepler:
  logs:
    buffer_size: 0
  hooks:
    pre_cleanup:
      run: |
        echo "PRE_CLEANUP_RAN" >> __MARKER_FILE__

services:
  cleanup-hook-service:
    command: ["sh", "-c", "echo Starting; sleep 300"]
