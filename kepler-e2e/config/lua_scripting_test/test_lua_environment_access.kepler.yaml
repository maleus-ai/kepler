kepler:
  logs:
    buffer_size: 0

services:
  lua-env-service:
    command: !lua |
      -- Access env table to get HOME
      local home = env.HOME or "/tmp"
      -- Generate a command that proves we got env access
      return {"sh", "-c", "echo ENV_ACCESS_WORKS HOME=" .. home .. "; sleep 300"}
