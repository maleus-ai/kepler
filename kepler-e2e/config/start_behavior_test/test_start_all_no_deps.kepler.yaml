kepler:
  logs:
    buffer_size: 0

services:
  base:
    command: ["sh", "-c", "echo BASE_STARTED; sleep 300"]
  dependent:
    command: ["sh", "-c", "echo DEPENDENT_STARTED; sleep 300"]
    depends_on:
      - base
