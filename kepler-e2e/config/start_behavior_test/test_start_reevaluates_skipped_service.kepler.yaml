kepler:
  logs:
    buffer_size: 0

services:
  always-run:
    command: ["sh", "-c", "echo ALWAYS_RUN_STARTED; sleep 300"]
  conditional:
    command: ["sh", "-c", "echo CONDITIONAL_STARTED; sleep 300"]
    if: false
