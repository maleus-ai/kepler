kepler:
  logs:
    buffer_size: 0

services:
  normal:
    command: ["sh", "-c", "echo NORMAL_STARTED; sleep 300"]
  disabled:
    command: ["sh", "-c", "echo DISABLED_STARTED; sleep 300"]
    if: false
