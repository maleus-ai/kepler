kepler:
  logs:
    buffer_size: 0

services:
  web:
    command: ["sh", "-c", "echo WEB_STARTED; sleep 300"]
  worker:
    command: ["sh", "-c", "echo WORKER_STARTED; sleep 300"]
    depends_on:
      - web
