kepler:
  autostart:
    environment:
      - KEPLER_TEST_VAR=lua_test_value

services:
  lua-checker:
    command: !lua |
      local val = kepler.env.KEPLER_TEST_VAR or "MISSING"
      return {"sh", "-c", "echo LUA_SAW=" .. val .. " && sleep 300"}
