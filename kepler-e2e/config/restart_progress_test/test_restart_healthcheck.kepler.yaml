# Service with healthcheck for testing restart reaches healthy
kepler:
  logs:
    buffer_size: 0

services:
  web:
    command: ["sh", "-c", "echo WEB_STARTED; sleep 300"]
    healthcheck:
      test: ["true"]
      interval: 500ms
      timeout: 2s
      retries: 1
      start_period: 0s
