# Test that recreate runs pre_start hooks with if condition (because state is cleared)
services:
  init-service:
    command: ["sh", "-c", "echo STARTED >> MARKER_FILE && sleep 3600"]
    hooks:
      pre_start:
        if: ${{ not service.initialized }}$
        run: "echo ON_INIT >> MARKER_FILE"
