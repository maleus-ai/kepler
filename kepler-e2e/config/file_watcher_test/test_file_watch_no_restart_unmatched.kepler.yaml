services:
  unmatched-watch-service:
    command: ["sh", "-c", "echo UNMATCHED_WATCH_START; sleep 300"]
    working_dir: __WATCH_DIR__
    restart:
      policy: always
      watch:
        - "*.txt"
