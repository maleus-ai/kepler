kepler:
  logs:
    buffer_size: 0

services:
  svc:
    command: ["sh", "-c", "echo SVC_START; sleep 300"]
    user: testuser1
    healthcheck:
      # No user override — should inherit testuser1 from service
      run: 'test "$(whoami)" = "testuser1"'
      interval: 2s
      timeout: 5s
      retries: 2
      start_period: 0s
