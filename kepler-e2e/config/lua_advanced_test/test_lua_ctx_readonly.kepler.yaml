kepler:
  logs:
    buffer_size: 0

services:
  ctx-readonly-service:
    environment: !lua |
      -- This should fail because ctx is read-only
      ctx.service_name = "hacked"
      return {"RESULT=should_not_reach"}
    command: ["sh", "-c", "echo $RESULT && sleep 30"]
    restart: no
