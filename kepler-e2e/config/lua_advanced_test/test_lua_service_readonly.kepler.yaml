kepler:
  logs:
    buffer_size: 0

services:
  service-readonly-service:
    environment: !lua |
      -- This should fail because service is read-only
      service.name = "hacked"
      return {"RESULT=should_not_reach"}
    command: ["sh", "-c", "echo $RESULT && sleep 30"]
    restart: no
