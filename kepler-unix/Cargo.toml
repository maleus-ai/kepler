[package]
name = "kepler-unix"
version.workspace = true
edition = "2024"

[target.'cfg(unix)'.dependencies]
libc = "0.2"
nix = { version = "0.31.1", features = ["user", "process", "fs", "signal"] }
sysinfo = "0.37"
tokio = { version = "1", features = ["process"] }
tracing = "0.1"
