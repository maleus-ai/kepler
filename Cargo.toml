[workspace]
members = [
    "kepler-cli",
    "kepler-protocol",
    "kepler-daemon",
    "kepler-exec",
    "kepler-unix",
    "kepler-tests",
    "kepler-e2e",
]
resolver = "2"

[workspace.package]
version = "0.5.1"

[profile.release]
strip = true
